// Mobile-specific logic: Close the modal when clicked outside of content but within the overlay
//		if (window.innerWidth <= 768) { // Check for mobile view
//			document.querySelector('.dashboard-modal-overlay').addEventListener('click', function (event) {
//				if (!event.target.closest('.dashboard-modal-content')) {
//					modal.style.display = "none";
//				}
